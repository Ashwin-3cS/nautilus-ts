{
  "name": "nautilus-ts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "build": "make",
    "start": "bun src/server.ts",
    "check": "bun scripts/check-reproducibility.ts",
    "prepare": "git config core.hooksPath .githooks"
  },
  "dependencies": {
    "@mysten/sui": "^2.6.0",
    "@noble/ed25519": "^2.3.0",
    "@noble/hashes": "^1.8.0",
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
